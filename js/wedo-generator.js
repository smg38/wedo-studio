const gen=(typeof javascript!== 'undefined'&& javascript.javascriptGenerator)? javascript.javascriptGenerator:(typeof javascriptGenerator!== 'undefined'? javascriptGenerator: null); if(!gen){ console.error('❌ ОШИБКА: javascriptGenerator не найден!');} else{ function addHighlight(block, code){ return `__highlightBlock('${block.id}');\n`+ code;} gen.forBlock['event_whenflagclicked']= function(block){ return addHighlight(block, '// Точка входа\n');}; gen.forBlock['wedo_connect']= function(block){ return addHighlight(block, 'await driver.connect();\n');}; gen.forBlock['wedo_motor_on']= function(block){ const port= parseInt(block.getFieldValue('PORT'), 10); const speedCode= gen.valueToCode(block, 'SPEED', gen.ORDER_ATOMIC)|| '0'; return addHighlight(block, `await driver.motorOn(${port}, ${speedCode});\n`);}; gen.forBlock['wedo_motor_off']= function(block){ const port= parseInt(block.getFieldValue('PORT'), 10); return addHighlight(block, `await driver.motorOff(${port});\n`);}; gen.forBlock['wedo_led_named']= function(block){ const color= block.getFieldValue('COLOR_NAME'); return addHighlight(block, `await driver.setLed(${color});\n`);}; gen.forBlock['wedo_led_variable']= function(block){ const colorCode= gen.valueToCode(block, 'COLOR', gen.ORDER_ATOMIC)|| '0'; return addHighlight(block, `await driver.setLed(${colorCode});\n`);}; gen.forBlock['wedo_wait']= function(block){ const secondsCode= gen.valueToCode(block, 'SECONDS', gen.ORDER_ATOMIC)|| '1'; return addHighlight(block, `await new Promise(r=> setTimeout(r, ${secondsCode}* 1000));\n`);}; gen.forBlock['wedo_sensor_motion']= function(block){ const code= 'Math.round((driver.getMotionDistance()?? 0)* 1.7)'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_sensor_distance_equal']= function(block){ const target= block.getFieldValue('DIST'); const code= '((()=>{\n'+ '    const __cm= Math.round((driver.getMotionDistance()??-1)* 1.7);\n'+ '    if(__cm< 0) return false;\n'+ `    if('${target}'=== 'near') return __cm>= 0&& __cm<= 5;\n`+ `    if('${target}'=== 'medium') return __cm>= 6&& __cm<= 11;\n`+ `    if('${target}'=== 'far') return __cm>= 12;\n`+ '    return false;\n'+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_sensor_tilt_pitch']= function(block){ const code= '(await(async()=>{\n'+ '    await driver.setTiltMode(0);\n'+ '    const __t= driver.getTiltAngle();\n'+ '    return __t? __t.x: 0;\n'+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_sensor_tilt_roll']= function(block){ const code= '(await(async()=>{\n'+ '    await driver.setTiltMode(0);\n'+ '    const __t= driver.getTiltAngle();\n'+ '    return __t? __t.y: 0;\n'+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_sensor_tilt_direction']= function(block){ const code= '(await(async()=>{\n'+ '    await driver.setTiltMode(1);\n'+ '    return driver.getTiltDirection()?? 0;\n'+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_sensor_tilt_equal']= function(block){ const target= block.getFieldValue('TILT'); const code= '(await(async()=>{\n'+ '    await driver.setTiltMode(1);\n'+ `    return(driver.getTiltDirection()??-1)=== ${target};\n`+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_debug_raw']= function(block){ const port= parseInt(block.getFieldValue('PORT'), 10); const code= '((()=>{\n'+ '    const __raw= driver.getRawValue('+ port+ ');\n'+ '    if(!__raw) return "(нет данных)";\n'+ '    return Array.from(__raw).map(b=> b.toString(16).padStart(2, "0")).join(" ");\n'+ '})())'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_debug_port_type']= function(block){ const port= parseInt(block.getFieldValue('PORT'), 10); const code= `(driver.getPortStatus(${port})|| "empty")`; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['wedo_debug_tilt_mode']= function(block){ const code= '(driver.getTiltMode()??-1)'; return[code, gen.ORDER_FUNCTION_CALL];}; gen.forBlock['controls_whileUntil']= function(block){ const until= block.getFieldValue('MODE')=== 'UNTIL'; const argument0= gen.valueToCode(block, 'BOOL', until? gen.ORDER_LOGICAL_NOT: gen.ORDER_NONE)|| 'false'; const branch= gen.statementToCode(block, 'DO'); const condition= until? `!(${argument0})`: `(${argument0})`; const highlighted= `__highlightBlock('${block.id}');\n`; return( highlighted+ `while(${condition}){\n`+ `  if(__shouldStop()) break;\n`+ `  await new Promise(r=> setTimeout(r, 20));\n`+ `${branch}`+ `}\n`);}; gen.forBlock['controls_repeat_ext']= function(block){ const repeats= gen.valueToCode(block, 'TIMES', gen.ORDER_ATOMIC)|| '0'; const branch= gen.statementToCode(block, 'DO'); const highlighted= `__highlightBlock('${block.id}');\n`; return( highlighted+ `for(let __i= 0; __i<(${repeats}); __i++){\n`+ `  if(__shouldStop()) break;\n`+ `  await new Promise(r=> setTimeout(r, 20));\n`+ `${branch}`+ `}\n`);}; console.log('✅[GENERATOR] wedo-generator.js v2.7.1 загружен');}